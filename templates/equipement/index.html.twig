{% extends 'base.html.twig' %}

{% block title %}Hello Equipement{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <button type="submit" class="btn btn-primary">valider</button>
    {{ form_end(form) }}
{% endblock %}
